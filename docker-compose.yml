version: "3"
services:
  worker:
    container_name: "netfoll-worker"
    build:
      context: .
    volumes:
      - worker:/data
    stop_signal: SIGINT
    restart: unless-stopped
    command: "python -m netfoll"
    ports:
      - "${EXTERNAL_PORT:-8080}:8080"

volumes:
  worker:
